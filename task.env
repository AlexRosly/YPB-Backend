name=AlexRosly
pass=YourPriceBookingDev
mongodb+srv://AlexRosly:YourPriceBookingDev@cluster0.2ybal2n.mongodb.net/test

mongodb+srv://AlexRosly:YourPriceBookingDev@cluster0.2ybal2n.mongodb.net/?retryWrites=true&w=majority

create 
    1.When Add language, objectType will be copies form EN and translate to new lang.

    {
    "type": "hotel",
    "bedrooms": 1,
    "detailedBedrooms": {
      "firstBedroom": {
        "doubleBed": 1,
        "singleBed": 1
      }
    },
    "roomServices":["KLLLL", "kkk"],
    "animals": {"smallDogSelected": true, "bigDogSelected": true},
    "description": "kmlmlm",
    "uniqueNumber": "kkoko",
    "totalSquare": 111,
    "smoking": true

}

1.робити профілі верифікатора
зберігати ІД, пошту (наразі фактичний доступ по пошті іде), 
фотографії шо він додав, обєкти які на верифікації у конкретного верифікатора. 
Та також відразу треба зробити список із ІД обєктів (напевно) які вже верифікував 
або не верифікував даний верифікатор (з тими обєктами з якими він працював)

---До БД доступ до адмінки додати зберігання фото
---Коли верифікатор бере один обєкт в роботу треба відмічати який саме він взяв роботу 
по ИД та більше не показувати в загальному списку обєктів на верифікацію, а показувати в його профілі

2.по кожному верифікатору робимо список обєктів які цей самий верифікатор відпрацював (верифікував чи не верифікував) 
Зберігаємо такі дані по кожному обєкту: ІД, назва, дата верифікації або не верифікації, ну і як пройшло - Verified or Not Verified

1. Показувати email в адмінці вже можна зараз. Так як адмін залогінився то у відповіді бека є email, тому фронт просто домалювує email
2. Стосовно аду та раю, зараз на беку так і є. Або верифіковано або видалено. Якщо щось з цього вибрано, то більше обєкт не вернеться
3. Яку саме інформацію маємо зберігати в профілі верифікатора?
4. Які документи верифікатора мють зберігатися на беці? Як саме ці документи будуть додаватися
5. До верифікованих обєктів треба додати id верифікатора, тоді в майбутньому можливо дивитися статистику хто верифікував та інше.
6. До видалених обєктів так само треба додати id, та змінювати статус на "deleted", 
а не видаляти зовсім як раніше було вирішено. Завантажені документи до обєктів з статусом "deleted" повністю видаляємо?
***видаляємо зовсім як вже вирішено (бо засруть базу) але зберігаємо лишень його ІД  
назву і статус шо він видаений. Це сберігаємо у профілі верифікатора.


і потрібно щоб ти ще добавив у відповідь 
https://server-your-price-booking.onrender.com/api/catalogForHotelier?language= . 
параметр який би показував чи був запит на 
https://server-your-price-booking.onrender.com/api/catalogForHotelier?language=  для цього міста/дістрікта.
тобто чи є створені сторінки